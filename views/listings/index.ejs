<% layout("./layouts/boilerplate.ejs") %>
<body>
  
  <div class="text-center my-4">
    <h1 class="display-5 fw-bold heading-gradient bulge-animation">
      🌍 Explore Amazing Listings
    </h1>
    <p class="text-muted mt-2">
      Find your next stay, adventure, or destination — all in one place.
    </p>
    <hr class="mx-auto" style="width: 120px; height: 3px; background: linear-gradient(90deg, #007bff, #6610f2, #e83e8c); border: none; border-radius: 2px;">
  </div>

  <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1">
    <% for(let listing of listingdata) { %>
      <a href="/listings/<%= listing._id %>" class="listing-link">
        <div class="card">
          <img src="<%= listing.image.url %>" class="card-img-top listing-card" alt="listing_image" style="height:16rem;">
          <div class="card-img-overlay"></div> 
          <div class="card-body">
            <p class="card-text">
              <b><%= listing.title %></b><br>
              &#8377;<%= Number(listing.price).toLocaleString("en-IN") %>
            </p>
          </div>
        </div>
      </a>
    <% } %>
  </div>


</body>






